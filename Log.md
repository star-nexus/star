# Log
same git commit but easy to read


## 2024-12-13
feat: 封装部队移动逻辑并支持多单位选择与特定地形移动规则

- 新增 army.py：将单位移动逻辑封装在 UnitController 类中
- 使用 environment_map 存储静态地形，unit_map 存储单位位置
- 支持通过 TAB 键在多个单位间切换，支持鼠标点击选择单位
- 根据单位类型 (ping, shui, shan) 实现不同的地形可进入规则
- 在被选中单位上方进行高亮显示，并在界面上显示当前选中单位信息
- 不再破坏环境地形，当单位移动时仅更新 unit_map，原始地形保持不变

feat: 支持通过方向键移动军队单位

- 在主循环中监听键盘事件（上下左右方向键）
- 实现选中一个部队单位（首个 R_ 或 W_ 开头的单位）进行移动
- 每次移动后更新 `map_matrix` 和重新渲染地图
- 简化地形恢复逻辑，将单位离开的位置重置为 `plain`"

feat: 使用 Pygame 实现地图生成器的可视化

- 添加 `game.py` 脚本，用于在 Pygame 窗口中渲染生成的地图
- 修改 `MapGenerator` 类以加载和渲染 Pygame 图块图像
- 更新图块资源格式为 `.png` 以支持透明度，并确保所有必要的图块图像存在于 `map_tiles` 目录中
- 增强地图数据生成逻辑，确保不同地形和单位正确显示
- 添加基本的 Pygame 主循环以保持窗口打开并处理关闭事件
