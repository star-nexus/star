# Log
same git commit but easy to read

## 2024-12-14
feat: 增强单位自动寻路功能并新增UI反馈

- 寻路与移动时考虑动态阻挡：若下个节点被单位占据会重新规划路径
- 在地图上高亮显示当前选中单位的规划路径，便于玩家直观理解其移动路线
- 在界面上显示鼠标所在格子坐标与当前目标点坐标，帮助玩家更好地理解地图位置信息


feat: 增加单位自动寻路功能

- 根据单位类型特性与可进入地形，为其规划一条可通行路径
- 使用 BFS 进行路径搜索，当指定目标不可达时自动选择最近可到达的点
- 新增 plan_path_to() 方法来为当前选中单位规划路径
- 新增 step_along_path() 方法让单位沿已规划路径逐步移动
- 可扩展为在实时游戏循环中自动执行寻路与移动逻辑


## 2024-12-13
feat: 增强地图生成逻辑与单位部署

- 环境与单位数据分离：`generate_map_data` 现返回 `environment_map` 与 `unit_map` 两个矩阵
- 支持自定义 R 与 W 两个阵营的单位数量，通过参数控制各方单位数量
- 新增河流上添加桥的功能：在 `river` 上随机生成 1~3 个 `bridge`
- 引入森林地形 `forest`：在地图上随机生成一定数量的森林格子
- 调整逻辑使后续对单位移动、地形交互更灵活，为后续扩展（如视野、战争迷雾、战斗规则）奠定基础

fix: 修复在所有单位阵亡时访问 selected_unit 导致 IndexError 的问题

- 在 UnitController.selected_unit 属性中加入安全检查，当 units_positions 为空或 selected_unit_index 超界时返回 None
- 确保当无单位可选时，不会再抛出 IndexError 异常
- 修改获胜提示到屏幕中央


feat: 扩大视野范围、隐藏不可见单位，并在阵营模式下实现胜负判定

- 视野范围从3x3扩大至5x5，以单位为中心上下左右各2格（共5x5）
- 在R/W阵营视角模式下，只有在可见区域内的单位和地形才显示，不可见区域内的敌军移动不可见
- 当所选阵营单位全部死亡时，对方阵营获胜，并在屏幕上显示胜利信息
- 当选中单位阵亡时自动选择同阵营其他单位，如果无可用单位则对方获胜

feat: 增加玩家模式、阵营视野与战争迷雾、部队战斗规则及模块化改进

- 启动时确定玩家模式（human或ai），暂时仅保存模式，不影响逻辑
- 区分R和W阵营，添加阵营间视野共享和战争迷雾模式
  - 视角模式1：上帝视角，全可见
  - 视角模式2：R阵营视野
  - 视角模式3：W阵营视野
  不在视野内的格子以黑色覆盖
- 以3*3范围为单位视野范围，多个单位的视野叠加
- 将单独的部队移动、战斗逻辑封装进UnitController类中
- 增加部队类型数据和战斗规则（ping、shui、shan互相克制关系）
- 不再改写环境地图，使用environment_map和unit_map分离地形与单位
- 部队移动遇敌时自动判定战斗，胜者留在格子中，败者被移除
- 增加forest地形作为示例

feat: 封装部队移动逻辑并支持多单位选择与特定地形移动规则

- 新增 army.py：将单位移动逻辑封装在 UnitController 类中
- 使用 environment_map 存储静态地形，unit_map 存储单位位置
- 支持通过 TAB 键在多个单位间切换，支持鼠标点击选择单位
- 根据单位类型 (ping, shui, shan) 实现不同的地形可进入规则
- 在被选中单位上方进行高亮显示，并在界面上显示当前选中单位信息
- 不再破坏环境地形，当单位移动时仅更新 unit_map，原始地形保持不变

feat: 支持通过方向键移动军队单位

- 在主循环中监听键盘事件（上下左右方向键）
- 实现选中一个部队单位（首个 R_ 或 W_ 开头的单位）进行移动
- 每次移动后更新 `map_matrix` 和重新渲染地图
- 简化地形恢复逻辑，将单位离开的位置重置为 `plain`"

feat: 使用 Pygame 实现地图生成器的可视化

- 添加 `game.py` 脚本，用于在 Pygame 窗口中渲染生成的地图
- 修改 `MapGenerator` 类以加载和渲染 Pygame 图块图像
- 更新图块资源格式为 `.png` 以支持透明度，并确保所有必要的图块图像存在于 `map_tiles` 目录中
- 增强地图数据生成逻辑，确保不同地形和单位正确显示
- 添加基本的 Pygame 主循环以保持窗口打开并处理关闭事件
